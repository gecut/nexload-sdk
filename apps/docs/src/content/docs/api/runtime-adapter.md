---
title: Runtime adapter
---

Runtime adapters provide time, uptime, runtime identity, memory snapshots, CPU snapshots, and optional shutdown signal handling.

Use `nodeRuntimeAdapter()` for Node and `bunRuntimeAdapter()` for Bun.
