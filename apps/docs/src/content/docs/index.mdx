---
title: Nexload SDK
description: Current documentation for ten focused Healthcheck and Payload CMS TypeScript packages.
sidebar:
  order: 1
topic: ecosystem
---

import { LinkButton } from "@astrojs/starlight/components";
import PackageGrid from "../../components/docs/PackageGrid.astro";
import { packageCatalog } from "../../lib/package-catalog";

Nexload SDK provides focused TypeScript packages for service health and Payload CMS. Install only the package that owns your task.

<p class="sl-flex sl-flex-wrap sl-gap-2">
  <LinkButton href="./start/choose-a-package/" icon="right-arrow">Choose a package</LinkButton>
  <LinkButton href="./packages/" variant="secondary">Browse all ten packages</LinkButton>
  <LinkButton href="./agents/install/" variant="minimal" icon="bot">Install agent skills</LinkButton>
</p>

## Healthcheck packages

<PackageGrid entries={packageCatalog.filter((entry) => entry.family === "healthcheck")} />

## Payload packages

<PackageGrid entries={packageCatalog.filter((entry) => entry.family === "payload")} />

## How to read these docs

Every package follows the same route: overview, installation, quick start, concepts, guides, API, examples, troubleshooting, migration, and compatibility. The displayed version is the current released version documented by this site.
