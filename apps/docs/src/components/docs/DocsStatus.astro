---
interface Props {
  status: "released" | "stable";
}

const { status } = Astro.props;
const label = status === "released" ? "Released" : "Stable";
---

<span class:list={["docs-status", `docs-status--${status}`]}>{label}</span>
