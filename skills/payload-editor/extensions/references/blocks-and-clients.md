# Blocks and client features

Use `BlocksFeature` for project Blocks such as Callout, Gallery, or Payload's experimental CodeBlock. The project owns schemas and migrations.

Client features require Payload import-map paths and JSON-safe props. Keep server functions and secrets behind authenticated endpoints. The v1 package root owns zero client JavaScript.
