# Preset versioning

Changing existing feature membership, default options, canonical order, or heading policy is a breaking major change. Adding a new opt-in preset can be minor when it does not alter existing output.

Document migrations from the old preset to explicit overrides. Never silently change a zero-argument default because no zero-argument default exists.
