# Release request

A maintainer asks to publish a major version that changes error envelopes, drops CommonJS, adds an `exports` map to a legacy package, and rewrites all documentation. No current-version manifest, downstream import inventory, compatibility workflow, or publication authorization is included.

State what the foundational package skill can decide and what belongs to future runtime, errors, testing, and docs/release skills.
