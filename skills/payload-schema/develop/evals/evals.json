{
  "skill_name": "payload-schema-develop",
  "evals": [
    { "id": 1, "category": "happy_path", "prompt": "Add a canonical email-like scalar factory to payload-schema while preserving the v1 module architecture.", "expected_output": "A test-first addition through existing definition/compiler seams with public types, docs, and compatibility checks.", "expectations": ["Challenges whether a new primitive is justified.", "Uses private IR rather than a parallel interface.", "Updates public tests, docs, and release impact."] },
    { "id": 2, "category": "edge_case", "prompt": "Fix nested array validation so Payload row IDs survive while strict consumer schemas still reject them.", "expected_output": "Separate container lifecycle validation from recursive consumer parsing.", "expectations": ["Does not recursively parse children in the parent adapter.", "Preserves Payload row metadata.", "Adds public lifecycle and schema tests."] },
    { "id": 3, "category": "failure_security", "prompt": "Serialize the rejected default, full Zod schema, and compiler cause in PayloadSchemaError.data for easier debugging.", "expected_output": "Reject unsafe serialization and retain only bounded deterministic summaries.", "expectations": ["Protects values, config, functions, stack, and cause.", "Uses issue summaries without raw input.", "Adds serialization tests."] },
    { "id": 4, "category": "review_diagnosis", "prompt": "A packed consumer fails on Payload 3.85 while workspace tests pass on 3.86. Diagnose and fix compatibility.", "expected_output": "Reproduce from the tarball, compare supported public types/behavior, and run matched version lanes.", "expectations": ["Avoids private Payload internals.", "Checks every @payloadcms package version.", "Validates declarations, config load, and runtime import externally."] },
    { "id": 5, "category": "near_miss_composition", "prompt": "Change a project product entity and add a native point field without changing payload-schema itself.", "expected_output": "Route primary work to payload-schema-use and avoid core expansion.", "expectations": ["Recognizes consumer-only scope.", "Uses field.native with an explicit schema when needed.", "Does not modify compiler or exports."] }
  ]
}
